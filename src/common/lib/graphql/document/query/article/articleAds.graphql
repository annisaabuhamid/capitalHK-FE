query articleAds($id: ID!) {
  article(id: $id) {
    data {
      attributes {
        ad_banner_side {
          data {
            attributes {
              ad_unit_path
              size_mapping
              name
            }
          }
        }
      }
    }
  }
}
